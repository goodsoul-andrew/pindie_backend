const authRouter = require('./auth');
const categoriesRouter = require('./categories');
const gamesRouter = require('./games');
const usersRouter = require('./users');

module.exports = { authRouter, categoriesRouter, gamesRouter, usersRouter };
